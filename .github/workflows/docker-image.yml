name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - uses: actions/checkout@v4

      # Step 2: Set up JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # Step 3: Grant execute permissions to Gradle wrapper
      - name: Grant execute permissions to Gradle wrapper
        run: chmod +x ./gradlew

      # Step 4: Build the JAR with Gradle
      - name: Build with Gradle
        run: ./gradlew clean build

      # Step 5: Read the current version
      - name: Read version
        id: read_version
        run: echo "VERSION=$(cat version.txt)" >> $GITHUB_ENV

      # Step 6: Build the Docker image with the version tag
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag mzhj19/cicd1:${{ env.VERSION }}

      # Step 7: Log in to Docker Hub
      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login --username "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # Step 8: Push the Docker image to Docker Hub
      - name: Push Docker image
        run: docker push mzhj19/cicd1:${{ env.VERSION }}

      # Step 9: Increment version number
      - name: Increment version
        id: increment_version
        run: |
          # Parse current version
          VERSION=$(cat version.txt)
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"

          # Increment the PATCH version
          PATCH=$((PATCH + 1))
          NEW_VERSION="$MAJOR.$MINOR.$PATCH"

          # Update version.txt
          echo "$NEW_VERSION" > version.txt

          # Output new version
          echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_ENV

      # Step 10: Commit and push updated version.txt
      - name: Commit updated version.txt
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add version.txt
          git commit -m "Increment version to ${{ env.NEW_VERSION }}"
          git push
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
